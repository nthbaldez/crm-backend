services: 
  postgres: 
    container_image: pg-crm-juridico
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD:
      POSTGRES_DB: crm-db
      PGDATA: /data/postgres
    volumes:
      - ./data/pg:/data/postgres
